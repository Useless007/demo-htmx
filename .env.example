# Example environment variables for demo-htmx
# DB path used by the app and tests (override for local runs)
DB_PATH=./data/app.db

# Server port
PORT=3000

# Lightweight rate limiter (requests per minute per client)
RATE_LIMIT_MAX=300

# Optional basic auth for protected debug endpoints (leave empty to disable)
#BASIC_AUTH_USERNAME=admin
#BASIC_AUTH_PASSWORD=changeme

# NOTE: Tests set a per-process DB_PATH automatically; you typically don't need to
# set DB_PATH when running the test suite (tests call resetDb()).
